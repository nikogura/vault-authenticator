package authenticator

import "testing"

func TestExec(t *testing.T) {
	// TODO implement this test by creating some data, and execing something like 'env' and checking it's output to see that the vars provided are there.
}
